import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  imageContainer: {
    width: 306,
    height: 205,
    alignSelf: 'center',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 50,
  },
});
